# tpm2tss-snapcraft

Snapcraft definition that stages OpenSSL with the TPM2-TSS engine and tools.

The engine is provided via the following staged packages:

- `libengine-tpm2-tss-openssl`
- `tpm2-tss-engine-tools`
- `tpm2-tools`
